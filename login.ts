let loginData = {
  Username: prompt("Enter a username: "),
  Password: prompt("Enter a password: ")
}

if (loginData.Username == loginData.Username && loginData.Password == loginData.Password) {
  console.log("Logged in as " + str(loginData.Username))
}
